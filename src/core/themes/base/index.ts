import { ModelOfTheme } from '@src/core/themes/model';
import { metrics } from './metrics';
import { fontSize } from './fontSize';
import { colors } from './colors';
import { fontFamily } from './fontFamily';

export const BaseTheme:ModelOfTheme = {
  metrics,
  fontSize,
  colors,
  fontFamily,
};
